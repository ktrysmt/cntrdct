[package]
name = "cntrdct-detector-config-interaction"
description = "Detect items carrying a structurally contradictory pair of cfg attributes."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cntrdct-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
tree-sitter = "0.22"
tree-sitter-rust = "0.21"
