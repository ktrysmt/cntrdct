// Source: https://github.com/azalea-rs/azalea/blob/86dc16c5de5d2368dba8504b86e98b468ee13af1/azalea-physics/src/collision/mod.rs
// License: MIT
// Note: minimal extract of one top-level `pub fn` from azalea-rs/azalea@86dc16c5de5d2368dba8504b86e98b468ee13af1 azalea-physics/src/collision/mod.rs (upstream lines 480-491, audit-corpus lines 5-16 after the 3-line header + 1 blank-line offset). The function carries a three-line `///` doc block (upstream lines 480-482 / corpus lines 5-7) whose third line reads `This is marked as deprecated in Minecraft.` but does NOT carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, batch-21 `mcgoo/vcpkg-rs` `probe_package` flag, batch-22 `overdrivenpotato/rust-vst2` `process_deprecated` flag, batch-23 `always-further/nono` `warn_for_deprecated_flags` flag, batch-24 `smol-machines/smolvm` `export_layer` flag, batch-25 `anyme123/Any-code` `decode_project_path` flag, batch-26 `autofitcloud/lsvine` `transform_readdir` flag, and batch-27 `reflex-search/reflex` `find_ruby_gem_names` flag. Diversifies `comment-code`'s Pattern C audit evidence from twelve upstreams to thirteen upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1 + nono 1 + smolvm 1 + Any-code 1 + lsvine 1 + reflex 1 + azalea 1) on thirteen unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics wrapper + Windows vcpkg bindings + VST 2.4 audio plugin host + capability-based sandbox CLI + portable lightweight VM image layer storage + Tauri-based Claude/Codex CLI session viewer + `tree -L 2`-style directory tree CLI iterator adapter + code-aware local code-search engine Ruby gemspec name extractor + Minecraft block-state physics / collision motion-blocking predicate). The function body falls into the in-tree-body sub-shape within Pattern C: it short-circuits on `BlockState::AIR` for the fast path and otherwise computes `legacy_calculate_solid(block) && registry_block != BlockKind::Cobweb && registry_block != BlockKind::BambooSapling` — the original Minecraft "motion blocking" predicate implemented in-tree against the crate's `BlockState` / `BlockKind` enums, with no Rust-side replacement function the doc steers callers toward. Within the in-tree-body sub-shape itself, azalea introduces a new structural variant — upstream-protocol-deprecation-reference: prior in-tree-body upstreams (batch-11 tls-parser `parse_tls_handshake_*next_protocol`, batch-12 glium `validate`, batch-20 sui mysten-metrics `channel`/`channel_with_total`, batch-24 smolvm `export_layer`, batch-25 Any-code `decode_project_path`, batch-26 lsvine `transform_readdir`) document a Rust-side replacement API (ALPN, the draw-parameter `Result` invariant, `monitored_mpsc::channel`, streaming export, `get_project_path_from_sessions`, `RDAdapter1`), whereas azalea's doc merely notes that the predicate's semantics are marked as deprecated in the underlying Minecraft game protocol (an external system, not a Rust-side replacement) — the function and its `legacy_*` sibling helpers are kept in-tree precisely to support legacy Minecraft worlds and clients that still rely on the old motion-blocking predicate. cntrdct's spec F5 Pattern C check does not interpret the doc's referent — only the case-folded `deprecated` substring matters — so the upstream-protocol-deprecation-reference case fires identically to prior in-tree-body cases with Rust-side replacements, confirming again the syntactic-only design (the same way batch-23 nono's meta-deprecation-warning-emitter, batch-26 lsvine's replacement-targets-a-struct, and batch-27 reflex's delegate-with-adapter-chain variants fire identically to their structural cousins). cntrdct's spec F5 Pattern C trigger fires on the case-folded `deprecated` substring; the preceding-siblings walker finds zero attribute items adjacent to the function (no `#[deprecated]`, no `#[doc(hidden)]`, no `#[track_caller]`, no `#[inline]` — the function carries no top-level attribute at all) so the `#[deprecated]` lint is not honoured. The function signature `pub fn legacy_blocks_motion(block: BlockState) -> bool` returns `bool` (a non-`Result`/`Option` type, the literal substrings `Result`/`Option` do not appear in the return-type text) so spec F3 Pattern A's return-type negation passes; the doc contains no Pattern A trigger phrase (none of `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none`) so Pattern A does not fire either way; the doc contains no `panic` substring so spec F4 Pattern B does not fire — only Pattern C fires. The function body contains no `unwrap` / `panic!` / `expect(` / `unreachable!` / `assert!` / `todo!` / `unimplemented!` / `debug_assert` body markers at all (only boolean-and / inequality comparisons against `BlockKind::Cobweb` and `BlockKind::BambooSapling` and an `==`-against-`BlockState::AIR` short-circuit), so even if the doc had a `panic` trigger Pattern B's body-marker negation would suppress it — but the doc has no `panic` substring, so the body-marker absence is moot here. Unrelated imports, the surrounding `legacy_calculate_solid` companion free function (upstream line 495, called from the body of `legacy_blocks_motion`), the `BlockState` / `BlockKind` enum definitions, the rest of the 508-line collision module, and the `azalea-physics` crate's other modules are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses unresolved identifiers (`BlockState`, `BlockKind`, `BlockTrait`, `legacy_calculate_solid`) syntactically without requiring resolution. azalea-rs/azalea is the Rust Minecraft bot / headless client framework domain (a high-performance Rust framework for creating Minecraft bots, with `azalea-physics` providing the block-state-aware collision and motion-blocking physics that lets bots navigate worlds the same way vanilla Minecraft clients do — the specific function is the bot-side reimplementation of Mojang's legacy `blocksMotion` predicate used by pathfinding and collision response), unrelated to the prior twelve Pattern C domains (Cardano Plutus-data, TLS NextProtocol, OpenGL draw-parameters, Unix pkg-config, async-channel metrics, Windows vcpkg, VST 2.4 audio plugin host, capability-based sandbox CLI, portable lightweight VM image layer storage, Tauri-based AI-coding-tool viewer, `tree -L 2`-style directory tree CLI, code-aware local code-search engine). SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Whether the block is treated as "motion blocking".
///
/// This is marked as deprecated in Minecraft.
pub fn legacy_blocks_motion(block: BlockState) -> bool {
    if block == BlockState::AIR {
        // fast path
        return false;
    }

    let registry_block = BlockKind::from(block);
    legacy_calculate_solid(block)
        && registry_block != BlockKind::Cobweb
        && registry_block != BlockKind::BambooSapling
}
