// Source: https://github.com/reflex-search/reflex/blob/2b312b3632c822126f4a2fa5af1a1f451caf9b9d/src/parsers/ruby.rs
// License: MIT
// Note: minimal extract of one top-level `pub fn` from reflex-search/reflex@2b312b3632c822126f4a2fa5af1a1f451caf9b9d src/parsers/ruby.rs (upstream lines 848-856, audit-corpus lines 5-13 after the 3-line header + 1 blank-line offset). The function carries a two-line `///` doc block (upstream lines 848-849 / corpus lines 5-6) whose second line reads `DEPRECATED: Use parse_all_ruby_projects() instead for monorepo support` but does NOT carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, batch-21 `mcgoo/vcpkg-rs` `probe_package` flag, batch-22 `overdrivenpotato/rust-vst2` `process_deprecated` flag, batch-23 `always-further/nono` `warn_for_deprecated_flags` flag, batch-24 `smol-machines/smolvm` `export_layer` flag, batch-25 `anyme123/Any-code` `decode_project_path` flag, and batch-26 `autofitcloud/lsvine` `transform_readdir` flag. Diversifies `comment-code`'s Pattern C audit evidence from eleven upstreams to twelve upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1 + nono 1 + smolvm 1 + Any-code 1 + lsvine 1 + reflex 1) on twelve unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics wrapper + Windows vcpkg bindings + VST 2.4 audio plugin host + capability-based sandbox CLI + portable lightweight VM image layer storage + Tauri-based Claude/Codex CLI session viewer + `tree -L 2`-style directory tree CLI iterator adapter + code-aware local code-search engine Ruby gemspec name extractor). The function body falls into the existing delegate-body sub-shape within Pattern C: it literally calls the replacement `parse_all_ruby_projects(root)` and threads the result through an iterator adapter chain — `.unwrap_or_default()` to flatten `Result<Vec<RubyProject>, _>` into `Vec<RubyProject>` by silently dropping any error, `.into_iter()` to consume the vector, `.map(|p| p.gem_name)` to extract each project's gem-name field, and `.collect()` to materialise the resulting `Vec<String>` — so the legacy entry point still works for callers that only need the gem name list while the replacement returns the full `RubyProject` records with their gemspec paths intact, suitable for the multi-package monorepo support the doc steers callers toward. This is the same body-shape category as batch-3 whisky-archive (`con_str` → `constr`), batch-13 pkg-config-rs (`find_library` → `probe_library`), and batch-21 vcpkg-rs (`probe_package` → `find_package`) — body delegates to (or is intended to be replaced by) the named replacement function — broadening delegate-body audit coverage from three upstreams to four while keeping Pattern C's body-shape footprint at the four shapes saturated by batches 22 and 23 (delegate-body, in-tree-body, stub-body, meta-deprecation-warning-emitter); the body-shape footprint within Pattern C therefore stays at four rather than growing to five. Within the delegate-body sub-shape itself, reflex introduces a new structural variant — delegate-with-result-flatten-and-field-extract: prior delegate-body upstreams adapt the replacement's return shape minimally (batch-3 whisky-archive `con_str` inlines an equivalent `json!({...})` literal without re-calling the replacement; batch-13 pkg-config-rs `find_library` adds `.map_err(|e| e.to_string())` to flatten the typed error into a string; batch-21 vcpkg-rs `probe_package` instantiates a fresh `Config::new().probe(name)` helper), whereas reflex chains four adapter steps (`.unwrap_or_default()` + `.into_iter()` + `.map(|p| p.gem_name)` + `.collect()`) on top of the literal replacement call to project the structured `Vec<RubyProject>` return down to the legacy `Vec<String>` shape while swallowing any `Err` outcome from the replacement. cntrdct's spec F5 Pattern C check does not interpret the body's adapter chain — it only inspects the doc and adjacent attributes — so the delegate-with-adapter-chain case fires identically to prior delegate-body cases with simpler adapters, confirming again the syntactic-only design. cntrdct's spec F5 Pattern C trigger fires on the case-folded `deprecated` substring; the preceding-siblings walker finds zero attribute items adjacent to the function (no `#[deprecated]`, no `#[doc(hidden)]`, no `#[track_caller]`, no `#[inline]` — the function carries no top-level attribute at all) so the `#[deprecated]` lint is not honoured. The function signature `pub fn find_ruby_gem_names(root: &std::path::Path) -> Vec<String>` returns `Vec<String>` (a non-`Result`/`Option` type, the literal substrings `Result`/`Option` do not appear in the return-type text) so spec F3 Pattern A's return-type negation passes; the doc contains no Pattern A trigger phrase (none of `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none`) so Pattern A does not fire either way; the doc contains no `panic` substring so spec F4 Pattern B does not fire — only Pattern C fires. Note that the function body contains the substring `unwrap` (in `.unwrap_or_default()` on the upstream-line-852 chained adapter) which would qualify as a Pattern B body marker if Pattern B's doc-trigger fired — but it does not, because the doc has no `panic` substring, so the body-marker negation is moot here; this is the same dual situation as batch-26 lsvine `transform_readdir` and the inverse of batch-17 wasmtime `roundtrip` where the doc does have `panic` and the body's `unwrap`/`assert_eq!` markers suppress Pattern B; reflex `find_ruby_gem_names` is the inverse — body has `unwrap` (via `unwrap_or_default`) but doc has no `panic` trigger, so the body marker is inert from cntrdct's perspective. Unrelated imports, the surrounding `RubyProject` struct + the companion `parse_all_ruby_projects` replacement free function (upstream line 821) + the lower-level `parse_gemspec_name` / `find_all_gemspec_files` helpers + the rest of the 1690-line Ruby parser module are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses unresolved identifiers (`parse_all_ruby_projects`, `RubyProject`) syntactically without requiring resolution. reflex-search/reflex is the code-aware local code-search engine domain (a Rust-built local-first code-search engine in the ripgrep / silver-searcher / ack lineage with project-structure awareness — homepage rfx-search.dev describes it as "The instant, code-aware local search engine"; the specific file is the Ruby project parser feeding the import-resolution pass), unrelated to the prior eleven Pattern C domains (Cardano Plutus-data, TLS NextProtocol, OpenGL draw-parameters, Unix pkg-config, async-channel metrics, Windows vcpkg, VST 2.4 audio plugin host, capability-based sandbox CLI, portable lightweight VM image layer storage, Tauri-based AI-coding-tool viewer, `tree -L 2`-style directory tree CLI). SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Find all Ruby gem names from gemspec files in the project (legacy version)
/// DEPRECATED: Use parse_all_ruby_projects() instead for monorepo support
pub fn find_ruby_gem_names(root: &std::path::Path) -> Vec<String> {
    parse_all_ruby_projects(root)
        .unwrap_or_default()
        .into_iter()
        .map(|p| p.gem_name)
        .collect()
}
