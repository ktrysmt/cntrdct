// Source: https://github.com/mcgoo/vcpkg-rs/blob/fa42994af157924e44223f2559a95c906e00c1fa/src/lib.rs
// License: MIT OR Apache-2.0
// Note: minimal extract of one top-level `pub fn` from mcgoo/vcpkg-rs@fa42994af157924e44223f2559a95c906e00c1fa src/lib.rs (upstream lines 291-295, audit-corpus lines 5-9 after the 3-line header + 1 blank-line offset). The function carries a single-line `///` doc block reading `Deprecated in favor of the find_package function` and a `#[doc(hidden)]` attribute, but does not carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, and batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag. Diversifies `comment-code`'s Pattern C audit evidence from five upstreams to six upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1) on six unrelated codebases. Structurally pairs with batch-13 pkg-config-rs as the Windows side of the build-script system-package binding family: pkg-config-rs covers Unix pkg-config tool bindings, vcpkg-rs covers Microsoft Windows vcpkg C/C++ package manager bindings. Both upstreams independently exhibit the same Pattern C bug shape (`/// Deprecated in favor of X` single-line doc + `#[doc(hidden)]` non-suppressive attribute + body delegating to the replacement function) on functionally-paired native-library-discovery crates targeting different platform ecosystems — exercising cntrdct's Pattern C check on cross-platform-paired upstreams that independently rediscovered the same `Deprecated`-prose-without-`#[deprecated]`-attribute oversight. The `#[doc(hidden)]` attribute does NOT suppress Pattern C because the walker checks the literal first identifier (`doc` vs. `deprecated`), confirming again that Pattern C distinguishes visibility-hiding from runtime-lint enforcement. Unrelated imports, struct / enum / impl definitions, the `Config::new().probe(name)` delegation target's own definition, and surrounding vcpkg-discovery helpers are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses unresolved type identifiers (`Library`, `Error`, `Config`) syntactically without requiring resolution. SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Deprecated in favor of the find_package function
#[doc(hidden)]
pub fn probe_package(name: &str) -> Result<Library, Error> {
    Config::new().probe(name)
}
