// Source: https://github.com/move-language/move/blob/c453c90994cba0af30314fe4134acee57fb20d0c/language/documentation/examples/diem-framework/crates/natives/src/account.rs
// License: Apache-2.0
// Note: minimal extract of one top-level `pub fn` from move-language/move@c453c90994cba0af30314fe4134acee57fb20d0c language/documentation/examples/diem-framework/crates/natives/src/account.rs (upstream lines 29-40, audit-corpus lines 5-16 after the 3-line header + 1 blank-line offset). The function carries a two-line `///` doc block (upstream lines 29-30 / corpus lines 5-6) reading `NOTE: this function will be deprecated after the Diem v3 release, but must / remain for replaying old transactions` but does NOT carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, batch-21 `mcgoo/vcpkg-rs` `probe_package` flag, batch-22 `overdrivenpotato/rust-vst2` `process_deprecated` flag, batch-23 `always-further/nono` `warn_for_deprecated_flags` flag, batch-24 `smol-machines/smolvm` `export_layer` flag, batch-25 `anyme123/Any-code` `decode_project_path` flag, batch-26 `autofitcloud/lsvine` `transform_readdir` flag, batch-27 `reflex-search/reflex` `find_ruby_gem_names` flag, batch-28 `azalea-rs/azalea` `legacy_blocks_motion` flag, and batch-29 `SharpCoder/teensycore` `write_byte` flag. Diversifies `comment-code`'s Pattern C audit evidence from fourteen upstreams to fifteen upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1 + nono 1 + smolvm 1 + Any-code 1 + lsvine 1 + reflex 1 + azalea 1 + teensycore 1 + move-language 1) on fifteen unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics wrapper + Windows vcpkg bindings + VST 2.4 audio plugin host + capability-based sandbox CLI + portable lightweight VM image layer storage + Tauri-based Claude/Codex CLI session viewer + `tree -L 2`-style directory tree CLI iterator adapter + code-aware local code-search engine Ruby gemspec name extractor + Minecraft block-state physics / collision motion-blocking predicate + bare-metal Teensy 4.x ARM Cortex-M7 microcontroller kernel-level memory-mapped peripheral I/O helper + Move language Diem-framework blockchain native-function signer-destructor). The function body falls into the in-tree-body sub-shape within Pattern C: two `debug_assert!(...)` invariants followed by `Ok(NativeResult::ok(213.into(), smallvec![]))` constructing the Move VM's native return value (gas cost `213.into()` + empty `smallvec![]` return values) with no Rust-side delegation to any replacement function — the body itself encodes the Move-VM signer-destruction protocol contract (a no-op other than the gas-cost accounting). This is the same body-shape category as batch-11 tls-parser (`parse_tls_handshake_*next_protocol`), batch-12 glium (`validate`), batch-20 sui mysten-metrics (`channel` / `channel_with_total`), batch-24 smolvm (`export_layer`), batch-25 Any-code (`decode_project_path`), batch-26 lsvine (`transform_readdir`), batch-28 azalea (`legacy_blocks_motion`), and batch-29 teensycore (`write_byte`) — body retains the original implementation in-tree rather than delegating to a replacement — broadening in-tree-body audit coverage from eight upstreams to nine while keeping Pattern C's body-shape footprint at the four shapes saturated by batches 22 and 23 (delegate-body, in-tree-body, stub-body, meta-deprecation-warning-emitter). Within the in-tree-body sub-shape itself, move-language introduces a new structural variant — future-tense-conditional-deprecation-with-replay-retention-rationale: prior in-tree-body upstreams either document the function as ALREADY deprecated in present tense and name an in-Rust replacement the doc steers callers toward (batch-11 tls-parser `Deprecated in favour of ALPN.`, batch-12 glium `DEPRECATED. Checks parameters...`, batch-20 sui `Deprecated: use monitored_mpsc::channel instead.`, batch-24 smolvm `DEPRECATED: Prefer streaming export via find_layer_path() + piped tar.`, batch-25 Any-code `DEPRECATED: Use get_project_path_from_sessions instead when possible`, batch-26 lsvine `DEPRECATED in favor of RDAdapter1`), reference an upstream-protocol-level deprecation source (batch-28 azalea `This is marked as deprecated in Minecraft.`), or use future tense with a self-equivalent rename replacement (batch-29 teensycore `This method will be deprecated in the future, in preference of `assign_8``), whereas move-language's doc reads `NOTE: this function will be deprecated after the Diem v3 release, but must remain for replaying old transactions` — FUTURE-TENSE deprecation conditionally tied to a SPECIFIC release version (Diem v3) AND coupled with an EXPLICIT retention rationale (replay of old transactions) AND naming NO replacement function (the doc does not point callers anywhere; the function is permanently preserved as part of the historical transaction-replay protocol surface). This contrasts every prior in-tree-body case: present-tense unconditional deprecations either name a replacement (tls-parser ALPN, glium parameter-checking, sui monitored_mpsc, smolvm streaming, Any-code session-derived, lsvine `RDAdapter1`) or reference an upstream-protocol-level source (azalea Minecraft), and the only prior future-tense case (teensycore) named a pure rename replacement (`assign_8`) without conditional language or retention rationale. cntrdct's spec F5 Pattern C check does not interpret tense, conditional release-version anchoring, retention rationale, or replacement-name presence — only the case-folded `deprecated` substring matters — so the future-tense-conditional-deprecation-with-replay-retention-rationale case fires identically to past-tense deprecation-with-named-replacement cases, confirming again the syntactic-only design (the same way batch-23 nono's meta-deprecation-warning-emitter, batch-26 lsvine's replacement-targets-a-struct, batch-27 reflex's delegate-with-adapter-chain, batch-28 azalea's upstream-protocol-deprecation-reference, and batch-29 teensycore's forward-looking-deprecation-with-self-equivalent-rename-replacement variants fire identically to their structural cousins). The function carries no top-level attribute at all (no `#[deprecated]`, no `#[doc(hidden)]`, no `#[track_caller]`, no `#[inline]` — the companion `native_create_signer` at upstream line 14 similarly carries no top-level attribute and no `///` doc block at all) so the preceding-siblings walker finds zero attribute items adjacent to `native_destroy_signer` and the `#[deprecated]` lint is not honoured. The function signature `pub fn native_destroy_signer(_context: &mut NativeContext, ty_args: Vec<Type>, arguments: VecDeque<Value>) -> PartialVMResult<NativeResult>` returns `PartialVMResult<NativeResult>` (the literal substring `Result` appears in `PartialVMResult` so spec F3 Pattern A's return-type negation suppresses Pattern A regardless of trigger phrase) and the doc contains no Pattern A trigger phrase (none of `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none`) so Pattern A does not fire either way; the doc contains no `panic` substring so spec F4 Pattern B does not fire — only Pattern C fires. The function body contains `debug_assert!` body markers (two of them, both gating native-function-call-protocol invariants — `ty_args.is_empty()` and `arguments.len() == 1`) which ARE members of cntrdct's PATTERN_B_BODY_MARKERS substring set, so even if the doc had a `panic` trigger Pattern B's body-marker negation would suppress Pattern B — but the doc has no `panic` substring, so the body-marker presence is moot here (different from batch-28 azalea and batch-29 teensycore where the body lacks both `unwrap`/`debug_assert` AND the doc lacks `panic`, the same dual situation as batch-17 wasmtime where the doc has `panic` and the body's `unwrap`/`assert_eq!` suppress Pattern B except inverted on the doc side — here the body markers are present but the doc trigger is absent so Pattern B is doubly suppressed). The `Ok(NativeResult::ok(213.into(), smallvec![]))` construct encodes the Move VM's native-function return-value protocol (the gas cost `213` is a hardcoded historical constant matching `native_create_signer`'s `25` counterpart and the empty `smallvec![]` return-values vector reflects the no-op nature of a signer destructor in the Move type system); cntrdct's spec F5 ignores body semantics entirely — the audit entry exists because the deprecation prose lacks the `#[deprecated]` attribute, not because of any Move-VM protocol concern. Unrelated imports (`use move_binary_format::errors::PartialVMResult; use move_core_types::account_address::AccountAddress; use move_vm_runtime::native_functions::NativeContext; use move_vm_types::{loaded_data::runtime_types::Type, natives::function::NativeResult, pop_arg, values::Value}; use smallvec::smallvec; use std::collections::VecDeque;`), the surrounding companion function `native_create_signer` (called by Move VM native-function dispatch alongside `native_destroy_signer` to provide signer-creation symmetry on the same gas-cost-table cohort), the Move-VM `NativeContext` / `NativeResult` / `PartialVMResult` types, the rest of the diem-framework natives crate (account, comparator, debug, event, hash, signature, signer, vector — the native-function dispatch surface for the Diem standard library), and the rest of the Move language reference compiler / VM are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses the function syntactically without requiring resolution of any imports or sibling helpers. move-language/move is the Move language reference compiler / VM domain (a Rust implementation of the Move smart-contract language originally developed by Diem / Libra, providing the bytecode format, type checker, native-function dispatch, and VM runtime for Move smart-contracts running on Diem-derivative blockchains — the specific function is the legacy signer-destructor native exposed through the Diem-framework native-functions crate, preserved as part of the historical transaction-replay protocol surface for nodes replaying pre-v3 Diem transactions during chain synchronisation), unrelated to the prior fourteen Pattern C domains (Cardano Plutus-data, TLS NextProtocol, OpenGL draw-parameters, Unix pkg-config, async-channel metrics, Windows vcpkg, VST 2.4 audio plugin host, capability-based sandbox CLI, portable lightweight VM image layer storage, Tauri-based AI-coding-tool viewer, `tree -L 2`-style directory tree CLI, code-aware local code-search engine, Minecraft block-state physics, bare-metal Teensy 4.x microcontroller peripheral I/O). SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// NOTE: this function will be deprecated after the Diem v3 release, but must
/// remain for replaying old transactions
pub fn native_destroy_signer(
    _context: &mut NativeContext,
    ty_args: Vec<Type>,
    arguments: VecDeque<Value>,
) -> PartialVMResult<NativeResult> {
    debug_assert!(ty_args.is_empty());
    debug_assert!(arguments.len() == 1);

    Ok(NativeResult::ok(213.into(), smallvec![]))
}
