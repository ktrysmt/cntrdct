// Source: https://github.com/readur/readur/blob/9966a79c28d7eddf619b04858638ca18f8fd129c/src/test_helpers.rs
// License: MIT
// Note: minimal extract of one top-level `pub async fn` from readur/readur@9966a79c28d7eddf619b04858638ca18f8fd129c src/test_helpers.rs (upstream lines 441-446, audit-corpus lines 5-10 after the 3-line header + 1 blank-line offset). The function carries a two-line `///` doc block (upstream lines 441-442 / corpus lines 5-6) reading `Backward compatibility wrapper that panics on error (to maintain existing test compatibility)\n/// DEPRECATED: Tests should migrate to use the Result-returning versions` but does NOT carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, batch-21 `mcgoo/vcpkg-rs` `probe_package` flag, batch-22 `overdrivenpotato/rust-vst2` `process_deprecated` flag, batch-23 `always-further/nono` `warn_for_deprecated_flags` flag, batch-24 `smol-machines/smolvm` `export_layer` flag, batch-25 `anyme123/Any-code` `decode_project_path` flag, batch-26 `autofitcloud/lsvine` `transform_readdir` flag, batch-27 `reflex-search/reflex` `find_ruby_gem_names` flag, batch-28 `azalea-rs/azalea` `legacy_blocks_motion` flag, batch-29 `SharpCoder/teensycore` `write_byte` flag, batch-30 `move-language/move` `native_destroy_signer` flag, and batch-31 `treeverse/lakeFS` `internal_delete_garbage_collection_rules` flag. Diversifies `comment-code`'s Pattern C audit evidence from sixteen upstreams to seventeen upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1 + nono 1 + smolvm 1 + Any-code 1 + lsvine 1 + reflex 1 + azalea 1 + teensycore 1 + move-language 1 + lakeFS 1 + readur 1) on seventeen unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics wrapper + Windows vcpkg bindings + VST 2.4 audio plugin host + capability-based sandbox CLI + portable lightweight VM image layer storage + Tauri-based Claude/Codex CLI session viewer + `tree -L 2`-style directory tree CLI iterator adapter + code-aware local code-search engine Ruby gemspec name extractor + Minecraft block-state physics / collision motion-blocking predicate + bare-metal Teensy 4.x ARM Cortex-M7 microcontroller kernel-level memory-mapped peripheral I/O helper + Move language Diem-framework blockchain native-function signer-destructor + data-lake-object-store Git-like-version-control OpenAPI-generated Rust client garbage-collection-rules deletion + self-hosted Rust/Axum document-management-with-OCR web app test-helper backwards-compatibility wrapper). The function signature is `pub async fn create_test_app_state_legacy() -> Arc<AppState>` — an ASYNCHRONOUS function (the `async` keyword precedes `fn`). This is the SECOND async-fn entry within cntrdct's Pattern C audit evidence: batch-31 lakeFS `internal_delete_garbage_collection_rules` was the FIRST async-fn entry, and readur lifts async-fn upstream coverage from one upstream (lakeFS only) to two (lakeFS + readur). tree-sitter-rust parses both `pub fn` and `pub async fn` as `function_item` nodes (the `async` keyword surfaces as a separate child node inside the `function_modifiers` field rather than altering the node kind), so cntrdct's spec F5 walker fires identically on both — broadening the lexical async-fn footprint from a single upstream-domain anchor to a two-upstream-domain anchor. The function body is `create_test_app_state().await\n        .expect("Failed to create test app state - check database connection")` — a single-step delegation to the named replacement `create_test_app_state` followed by `.expect(...)` to convert the replacement's `Result<Arc<AppState>, TestHelperError>` return into the deprecated function's `Arc<AppState>` return by panicking on the `Err` arm. This is the delegate-body sub-shape within Pattern C, contrasting batch-22 rust-vst2 stub-body, batch-23 nono meta-deprecation-warning-emitter, and the in-tree-body cases (batch-11 tls-parser, batch-12 glium, batch-20 sui mysten-metrics, batch-24 smolvm, batch-25 Any-code, batch-26 lsvine, batch-28 azalea, batch-29 teensycore, batch-30 move-language, batch-31 lakeFS) — broadens delegate-body audit coverage from four upstreams (whisky-archive con_str→constr, pkg-config-rs find_library→probe_library, vcpkg-rs probe_package→find_package, reflex find_ruby_gem_names→parse_all_ruby_projects) to five without introducing a new Pattern C body-shape sub-shape (the four shapes saturated by batches 22 and 23 — delegate-body, in-tree-body, stub-body, meta-deprecation-warning-emitter — stay at four). Within the delegate-body sub-shape itself, readur introduces a new panic-on-error-wrapper-for-backwards-compat-via-.expect() structural variant: the deprecated function exists specifically to maintain a legacy non-Result-returning interface that pre-dates the migration to fallible Result-returning replacements, and the body uses `.expect(...)` to discard the replacement's `Result<T, E>` return and re-expose a `T` return that panics on `Err`. This contrasts every prior delegate-body case: batch-3 whisky-archive `con_str` → `constr` is a NO-OP return-type-preserving rename (both return `Value`), batch-13 pkg-config-rs `find_library` → `probe_library` is a single-step ERROR-TYPE-ADAPTER via `.map_err(|e| e.to_string())` (both return `Result`, only the error type changes from a struct to a `String`), batch-21 vcpkg-rs `probe_package` → `find_package` is a single-step FRESH-HELPER instantiation via `Config::new().probe(name)` (both return `Result`, the deprecated form pre-dates the builder pattern), batch-27 reflex `find_ruby_gem_names` → `parse_all_ruby_projects` is a four-step SHAPE-PROJECTION via `.unwrap_or_default().into_iter().map(|p| p.gem_name).collect()` (replacement returns `Result<Vec<RubyProject>>`, deprecated returns `Vec<String>` by silently discarding the `Err` arm AND projecting each `RubyProject` to its `.gem_name` String field). readur's panic-wrapper is the first sub-shape where the deprecated function explicitly preserves a panic-on-error semantic that the replacement migrated away from — the test-helper legacy interface choice is intentional, not accidental (the doc explicitly says "to maintain existing test compatibility"), so the panic-conversion is a load-bearing semantic property of the deprecated function rather than an error-handling oversight. cntrdct's spec F5 Pattern C check does not interpret body semantics, replacement-side return-type evolution, or the deliberate-vs-accidental nature of the deprecated function's continued existence — only the case-folded `deprecated` substring matters — so the panic-on-error-wrapper-for-backwards-compat-via-.expect() case fires identically to prior delegate-body cases that preserve, adapt, or discard the replacement's return type, confirming again the syntactic-only design. The function carries no top-level attribute at all (no `#[deprecated]`, no `#[doc(hidden)]`, no `#[track_caller]`, no `#[inline]` — the readur test-helper module is hand-authored test infrastructure rather than generator-emitted code like lakeFS, but follows the same attribute-less convention), so `preceding_siblings_have_deprecated` in `src/detectors/comment_code.rs` finds zero attribute items adjacent to the function and the `#[deprecated]` lint is not honoured. The function signature returns `Arc<AppState>` (the literal substrings `Result` and `Option` are both ABSENT from the return type so spec F3 Pattern A's return-type negation does NOT fire) and the doc contains no Pattern A trigger phrase (none of `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none`) so Pattern A does not fire either way; the doc DOES contain the substring `panic` (in `panics on error`) so spec F4 Pattern B's doc trigger fires, but the body contains `.expect(` which IS a member of cntrdct's PATTERN_B_BODY_MARKERS substring set (`unwrap` / `panic!` / `expect(` / `unreachable!` / `assert!` / `todo!` / `unimplemented!` / `debug_assert`), so spec F4 Pattern B's body-marker negation suppresses Pattern B — only spec F5 Pattern C fires. This is the FIRST audit entry where the doc has BOTH `panic` AND `deprecated` triggers AND the body contains a PATTERN_B_BODY_MARKER substring — demonstrating that cntrdct's Pattern B and Pattern C checks operate independently of each other (Pattern B's doc-trigger fires AND body-marker negation suppresses, while Pattern C's doc-trigger fires AND attribute-negation does not suppress). Prior Pattern B/C interaction cases are all single-trigger-firing: batch-14 zarrs / batch-16 parking_lot_core / batch-19 vortex-buffer fire only Pattern B (doc has `panic`, body has no marker, no `deprecated` in doc); batch-15 boundless / batch-17 wasmtime / batch-18 rust-s3 fire only Pattern A (doc has `may fail`, signature is non-Result, no `panic` or `deprecated` interactions); batches 3 / 11 / 12 / 13 / 20-31 fire only Pattern C (doc has `deprecated`, no `panic` substring); batch-17 wasmtime is the only prior case where doc has BOTH a Pattern A trigger AND a Pattern B trigger (doc says `may fail` AND `# Panics`), but the body has `unwrap`/`assert_eq!` Pattern B markers and the signature `() -> ()` is non-Result so Pattern A fires AND Pattern B is suppressed — and the doc lacks `deprecated` so Pattern C does not enter the picture. readur is the first three-way doc/body interaction case where Pattern B is suppressed AND Pattern C fires, completing the doc-trigger × body-marker × attribute-negation interaction matrix for Pattern B/C cross-effects. The replacement function `create_test_app_state` is itself a sibling top-level `pub async fn` in the same `src/test_helpers.rs` file (upstream line 356) carrying a `///` doc block that says `Creates a test AppState with default configuration and services / This provides a convenient way to get a fully configured AppState for testing` and a body that delegates to `create_test_app_state_with_options(TestAppStateOptions::default()).await` — the replacement does NOT itself contain the substrings `deprecated` or `panic` in its doc, so spec F5 does not fire on it; the replacement is itself a thin wrapper around the underlying `create_test_app_state_with_options` helper that opens the actual database connection and constructs the `AppState` struct. The replacement's NAME-IS-A-PREFIX-OF-DEPRECATED relationship (`create_test_app_state` is a prefix of `create_test_app_state_legacy`) is structurally a `_legacy`-suffix legacy-rename — the deprecated function is the prior canonical name with a `_legacy` suffix appended after the migration to the Result-returning replacement claimed the original name. This is structurally distinct from prior named-replacement Pattern C cases where the replacement's name was either a wholly different function name (whisky-archive `con_str` → `constr` is a underscore-stripped rename; pkg-config-rs `find_library` → `probe_library` is a verb swap; vcpkg-rs `probe_package` → `find_package` is a verb swap; sui `channel` → `monitored_mpsc::channel` is a module-path-prefixed rename; Any-code `decode_project_path` → `get_project_path_from_sessions` is a method-shape change; lsvine `transform_readdir` → `RDAdapter1` is a function-to-struct change; reflex `find_ruby_gem_names` → `parse_all_ruby_projects` is a method-shape change; teensycore `write_byte` → `assign_8` is a complete rename; lakeFS `internal_delete_garbage_collection_rules` → `deleteGCRules` is a snake_case→camelCase change), the deprecated form is the SHORTER name in nearly every case (because the canonical-name convention chose the cleaner identifier); readur is the FIRST case where the deprecated form is the LONGER name (`_legacy` suffix appended) because the migration claimed the canonical name for the new fallible-Result-returning version. cntrdct's spec F5 ignores all naming conventions; the audit entry exists because the deprecation prose lacks the `#[deprecated]` attribute, not because of any naming-convention concern. The sibling deprecated functions `create_test_app_state_with_config_legacy` (upstream line 450), `create_test_app_state_with_upload_path_legacy` (upstream line 457), and `create_test_app_state_with_user_watch_legacy` (upstream line 464) share the same panic-on-error-wrapper-for-backwards-compat-via-.expect() sub-shape with different parameter signatures (taking `config: Config`, `upload_path: &str`, `user_watch_base_dir: &str` respectively) — each carries the same `/// Backward compatibility wrapper that panics on error / /// DEPRECATED: Tests should migrate to use the Result-returning versions` doc and the same `.await.expect(...)` body shape, and each would be a second / third / fourth Pattern C TP cntrdct would surface from the same file. The non-legacy parameter-only variant `create_test_app_state_with_config` (upstream line 364) ALSO carries a `/// DEPRECATED: Use create_test_app_state_with_options instead for better flexibility` doc but its body is in-tree-body (builds DB+services directly, returns `Result`) rather than the panic-wrapper delegate-body — it would be a fifth Pattern C TP on the same file representing the in-tree-body Pattern C variant in readur, kept out of the current minimal extract because cntrdct's spec F5 fires per-function and the panic-wrapper-via-.expect() sub-shape is the new diversification anchor for batch 32 (rather than re-covering the in-tree-body sub-shape lakeFS / move-language / teensycore / azalea already anchor). Unrelated imports (`use crate::auth::oidc::OidcClient; use crate::config::Config; use crate::db::Database; use crate::file_service::FileService; use crate::models::AppState; use crate::ocr::queue::OcrQueueService; use crate::sync::progress::SyncProgressTracker; use sqlx::PgPool; use std::sync::Arc;`), the sibling test-helper functions (`create_test_app_state` line 356, `create_test_app_state_with_config` line 364, `create_test_app_state_with_options` line 388, `create_test_app_state_with_upload_path` line 427, `create_test_app_state_with_user_watch` line 435, and their `_legacy` counterparts), the `TestAppStateOptions` builder struct, the `TestHelperError` enum, the `create_test_database` / `create_test_database_from_options` / `create_test_file_service` / `create_test_queue_service` helpers, and the rest of the readur test-infrastructure surface are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses the function syntactically without requiring resolution of any imports or sibling helpers. readur/readur is the self-hosted Rust/Axum document-management-with-OCR web app domain (an open-source self-hosted Paperless-ngx-alternative that stores documents on local disk / S3 / WebDAV, runs OCR via Tesseract / OCRmyPDF, indexes the extracted text in PostgreSQL with full-text search, and exposes a web UI for browsing / tagging / searching documents with OIDC authentication and per-user filesystem watch directories; the specific function is the legacy test-helper wrapper that constructs a fully-initialized `AppState` for integration tests by panicking on database-connection failure to preserve the pre-Result-migration test API), unrelated to the prior sixteen Pattern C domains (Cardano Plutus-data, TLS NextProtocol, OpenGL draw-parameters, Unix pkg-config, async-channel metrics, Windows vcpkg, VST 2.4 audio plugin host, capability-based sandbox CLI, portable lightweight VM image layer storage, Tauri-based AI-coding-tool viewer, `tree -L 2`-style directory tree CLI, code-aware local code-search engine, Minecraft block-state physics, bare-metal Teensy 4.x microcontroller peripheral I/O, Move language Diem-framework blockchain native-function dispatch, data-lake-object-store OpenAPI-generated Rust client). SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Backward compatibility wrapper that panics on error (to maintain existing test compatibility)
/// DEPRECATED: Tests should migrate to use the Result-returning versions
pub async fn create_test_app_state_legacy() -> Arc<AppState> {
    create_test_app_state().await
        .expect("Failed to create test app state - check database connection")
}
