// Source: https://github.com/anyme123/Any-code/blob/a8f361b4cc205b7201fa03f6bfe3da865dcd85b3/src-tauri/src/commands/claude/paths.rs
// License: MIT
// Note: minimal extract of one top-level `pub fn` from anyme123/Any-code@a8f361b4cc205b7201fa03f6bfe3da865dcd85b3 src-tauri/src/commands/claude/paths.rs (upstream lines 44-68, audit-corpus lines 5-29 after the 3-line header + 1 blank-line offset). The function carries a three-line `///` doc block (upstream lines 44-46 / corpus lines 5-7) whose third line reads `DEPRECATED: Use get_project_path_from_sessions instead when possible` but does NOT carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 sidan-lab/whisky-archive con_str* family, batch-11 rusticata/tls-parser `parse_tls_handshake_*next_protocol` family, batch-12 glium/glium `validate` flag, batch-13 rust-lang/pkg-config-rs `find_library` flag, batch-20 MystenLabs/sui `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, batch-21 mcgoo/vcpkg-rs `probe_package` flag, batch-22 overdrivenpotato/rust-vst2 `process_deprecated` flag, batch-23 always-further/nono `warn_for_deprecated_flags` flag, and batch-24 smol-machines/smolvm `export_layer` flag. Diversifies `comment-code`'s Pattern C audit evidence from nine upstreams to ten upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1 + nono 1 + smolvm 1 + Any-code 1) on ten unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics wrapper + Windows vcpkg bindings + VST 2.4 audio plugin host + capability-based sandbox CLI + portable lightweight VM image layer storage + Tauri-based Claude/Codex CLI session viewer project-path encoding). The function body falls into the existing in-tree-body sub-shape within Pattern C: it computes the decoded path via `encoded.replace('-', "/")` and then branches on `#[cfg(target_os = "windows")]` / `#[cfg(not(target_os = "windows"))]` block-level attribute items to produce either a back-slash-normalised Windows path (stripping the `\\?\` long-path prefix when present) or the slash-normalised non-Windows path, returning a `String` — the function continues to work and is the documented fallback path in the comment ("This is a fallback - the encoding isn't reversible when paths contain hyphens"), but the doc steers callers to the canonical `get_project_path_from_sessions` lookup that recovers the original path via session-file metadata rather than guessing from the encoded directory name. This is the same body-shape category as batch-11 tls-parser, batch-12 glium, batch-20 sui mysten-metrics, and batch-24 smolvm (in-tree implementation rather than delegation to the replacement), on a fifth unrelated upstream — broadening in-tree-body audit coverage from four upstreams to five without introducing a new Pattern C body-shape sub-shape; the body-shape footprint within Pattern C stays at the four shapes saturated by batches 22 and 23 (delegate-body, in-tree-body, stub-body, meta-deprecation-warning-emitter). The two `#[cfg(target_os = "...")]` attribute items live INSIDE the function body on block expressions (children of the body's block node), not as preceding siblings of the `function_item`, so `preceding_siblings_have_deprecated` in `src/detectors/comment_code.rs` does NOT see them when walking the file-scope siblings of the function — the walker stops at the first non-comment, non-attribute sibling and only inspects preceding `attribute_item` nodes at the top-level scope; in-body cfg attributes are inert from Pattern C's perspective, confirming again that Pattern C distinguishes attribute scope (function-level vs. block-level) at the syntactic walker boundary. cntrdct's spec F5 Pattern C trigger fires on the case-folded `deprecated` substring; the preceding-siblings walker finds zero attribute items adjacent to the function (no `#[deprecated]`, no `#[doc(hidden)]`, no `#[track_caller]` — the function carries no top-level attribute at all) so the `#[deprecated]` lint is not honoured. The function signature `pub fn decode_project_path(encoded: &str) -> String` returns `String` (a non-`Result`/`Option` type), so spec F3 Pattern A's return-type negation passes; the doc contains no Pattern A trigger phrase (none of `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none`) so Pattern A does not fire either way; the doc contains no `panic` substring so spec F4 Pattern B does not fire — only Pattern C fires. Unrelated imports (`anyhow::{Context, Result}`, `std::fs`, `std::path::PathBuf`), the surrounding `get_claude_dir` / `get_codex_dir` / `encode_project_path` / `normalize_path_for_comparison` helpers, and the `dirs::home_dir()` lookup the unrelated helpers depend on are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses unresolved identifiers (`String`, `target_os`) syntactically without requiring resolution. anyme123/Any-code is the Tauri-based AI-coding-tool viewer domain (a desktop GUI bundling Claude CLI + Codex CLI session browsing over `~/.claude/projects/`-style encoded project directories), unrelated to the prior nine Pattern C domains. SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Decodes a project directory name back to its original path
/// The directory names in ~/.claude/projects are encoded paths
/// DEPRECATED: Use get_project_path_from_sessions instead when possible
pub fn decode_project_path(encoded: &str) -> String {
    // This is a fallback - the encoding isn't reversible when paths contain hyphens
    // For example: -Users-mufeedvh-dev-jsonl-viewer could be /Users/mufeedvh/dev/jsonl-viewer
    // or /Users/mufeedvh/dev/jsonl/viewer
    let decoded = encoded.replace('-', "/");

    // On Windows, ensure we use backslashes for consistency
    #[cfg(target_os = "windows")]
    {
        let mut windows_path = decoded.replace('/', "\\");
        // Remove Windows long path prefix if present
        if windows_path.starts_with("\\\\?\\") {
            windows_path = windows_path[4..].to_string();
        }
        windows_path
    }

    #[cfg(not(target_os = "windows"))]
    {
        decoded
    }
}
