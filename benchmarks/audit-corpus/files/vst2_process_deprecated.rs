// Source: https://github.com/overdrivenpotato/rust-vst2/blob/244e14bd28caff3b21aa27f26a57bf01f01b7780/src/interfaces.rs
// License: MIT
// Note: minimal extract of one top-level `pub fn` from overdrivenpotato/rust-vst2@244e14bd28caff3b21aa27f26a57bf01f01b7780 src/interfaces.rs (upstream lines 16-17, audit-corpus lines 5-6 after the 3-line header + 1 blank-line offset). The function carries a single-line `///` doc block reading `Deprecated process function.` but does not carry the `#[deprecated]` runtime attribute the Rust deprecation lints honour, so downstream consumers receive no compiler warning — the textbook Tan SOSP 2007 §3.2 Pattern C ("bad comment": deprecation prose without `#[deprecated]` attribute) bug shape, the same one cntrdct's batch-3 `sidan-lab/whisky-archive` con_str* family, batch-11 `rusticata/tls-parser` `parse_tls_handshake_*next_protocol` family, batch-12 `glium/glium` `validate` flag, batch-13 `rust-lang/pkg-config-rs` `find_library` flag, batch-20 `MystenLabs/sui` `mysten-metrics/metered_channel` `channel`/`channel_with_total` flag, and batch-21 `mcgoo/vcpkg-rs` `probe_package` flag. Diversifies `comment-code`'s Pattern C audit evidence from six upstreams to seven upstreams (whisky-archive 4 + tls-parser 2 + glium 1 + pkg-config-rs 1 + sui mysten-metrics 2 + vcpkg-rs 1 + rust-vst2 1) on seven unrelated codebases (Cardano Plutus-data helpers + TLS NextProtocol parsers + OpenGL draw-parameter check + Unix pkg-config bindings + async-channel metrics + Windows vcpkg bindings + VST 2.4 audio plugin host). Introduces the stub-body Pattern C sub-shape: the function body is the empty block `{ }` retained as an ABI / API placeholder for callers that link against the old symbol while migrating to the replacement `process_replacing` / `process_replacing_f64` family, contrasting prior sub-shapes that delegate to the replacement (batch-3 whisky-archive con_str → constr, batch-13 pkg-config-rs find_library → probe_library, batch-21 vcpkg-rs probe_package → find_package) and prior sub-shapes that retain the original implementation in-tree (batch-11 tls-parser parse_tls_handshake_next_protocol, batch-12 glium validate, batch-20 sui mysten-metrics channel / channel_with_total). cntrdct's spec F5 Pattern C trigger fires on the `deprecated` substring; the preceding-siblings walker finds zero attributes adjacent to the function so the `#[deprecated]` lint is not honoured. The function signature has unit return so spec F3 Pattern A's return-type negation would pass on `returns err` / `returns result` / `may fail` / `fallible` / `returns option` / `may return none` triggers, but the doc contains no Pattern A trigger phrase so Pattern A does not fire; the doc contains no `panic` substring so spec F4 Pattern B does not fire — only Pattern C fires. Unrelated imports, the file-level `#![doc(hidden)]` inner attribute, the surrounding `process_replacing` / `process_replacing_f64` / `host_dispatch_*` function family, and the C ABI struct / enum definitions are dropped because comment-code v0 walks top-level `function_item` only and the doc-walk + Pattern C check operate per-fn; tree-sitter parses unresolved type identifiers (`AEffect`) syntactically without requiring resolution. SHA-256 is of the audit-corpus file as committed (minimal extract, per `benchmarks/audit-corpus/README.md` `Per-detector seed targets` item 4).

/// Deprecated process function.
pub fn process_deprecated(_effect: *mut AEffect, _inputs_raw: *mut *mut f32, _outputs_raw: *mut *mut f32, _samples: i32) { }
